# MindCache Cloud Configuration
# Configure specific instances from your project (no auto-creation)

# ============ SERVER-SIDE ONLY (never exposed to browser) ============
# API Key scoped to your project (generate at mindcache.io/settings/keys)
MINDCACHE_API_KEY=mc_dev_local

# Project ID (find in your project URL: mindcache.io/projects/[PROJECT_ID])
MINDCACHE_PROJECT_ID=your_project_id_here

# API URL (for server-side requests)
MINDCACHE_API_URL=http://localhost:8787

# ============ CLIENT-SIDE (safe to expose) ============
# WebSocket URL for real-time sync
NEXT_PUBLIC_MINDCACHE_API_URL=http://localhost:8787

# Project ID (for display purposes)
NEXT_PUBLIC_PROJECT_ID=your_project_id_here

# Instance IDs - get these from the MindCache web UI (localhost:3003)
# Create instances in your project, then copy their IDs here
NEXT_PUBLIC_INSTANCE_FORM=your_form_instance_id
NEXT_PUBLIC_INSTANCE_IMAGE=your_image_instance_id
NEXT_PUBLIC_INSTANCE_WORKFLOW=your_workflow_instance_id
NEXT_PUBLIC_INSTANCE_EDITOR=your_editor_instance_id
