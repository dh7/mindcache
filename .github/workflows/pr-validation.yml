name: PR Validation

on:
  pull_request:
    branches: [ main ]
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  validate:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9.15.0
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        cache: 'pnpm'
    
    - name: Install dependencies
      run: pnpm install --frozen-lockfile --ignore-scripts
    
    - name: Cache Turborepo
      uses: actions/cache@v4
      with:
        path: .turbo
        key: turbo-${{ runner.os }}-node20-${{ github.head_ref }}-${{ github.sha }}
        restore-keys: |
          turbo-${{ runner.os }}-node20-${{ github.head_ref }}-
          turbo-${{ runner.os }}-node20-main-
    
    - name: 🔍 Lint check
      run: |
        echo "Running ESLint..."
        pnpm run lint
        echo "✅ Linting passed!"
    
    - name: 🧪 Test check  
      run: |
        echo "Running tests..."
        pnpm test
        echo "✅ All tests passed!"
    
    - name: 🏗️ Build check
      run: |
        echo "Building project..."
        pnpm run build
        echo "✅ Build successful!"
      env:
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_dummy-key-for-ci-build
        CLERK_SECRET_KEY: sk_test_dummy-key-for-ci-build
    
    - name: 📊 Test coverage
      run: |
        echo "Running tests..."
        pnpm test
    
    - name: 🔒 Security check
      run: |
        echo "Running security audit..."
        pnpm audit --audit-level=moderate
        echo "✅ Security check passed!"
    
    - name: ✅ All checks passed
      run: |
        echo "🎉 All validation checks passed!"
        echo "PR is ready for review and merge."

  size-check:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: Install pnpm
      uses: pnpm/action-setup@v4
      with:
        version: 9.15.0
    
    - name: Use Node.js 20.x
      uses: actions/setup-node@v4
      with:
        node-version: 20.x
        cache: 'pnpm'
    
    - name: Install dependencies
      run: pnpm install --frozen-lockfile --ignore-scripts
    
    - name: Cache Turborepo
      uses: actions/cache@v4
      with:
        path: .turbo
        key: turbo-${{ runner.os }}-node20-${{ github.head_ref }}-${{ github.sha }}
        restore-keys: |
          turbo-${{ runner.os }}-node20-${{ github.head_ref }}-
          turbo-${{ runner.os }}-node20-main-
    
    - name: Build and check bundle size
      run: |
        pnpm run build
        echo "📦 Checking bundle sizes..."
        du -sh packages/mindcache/dist/
        du -sh packages/shared/dist/
      env:
        NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY: pk_test_dummy-key-for-ci-build
        CLERK_SECRET_KEY: sk_test_dummy-key-for-ci-build
        
    - name: Package size check
      run: |
        cd packages/mindcache && pnpm pack && ls -lh *.tgz && rm *.tgz
        echo "✅ Package size check complete!"

  docs-check:
    runs-on: ubuntu-latest
    if: github.event.pull_request.draft == false
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
    
    - name: 📄 Build GitHub Pages (Jekyll)
      uses: actions/jekyll-build-pages@v1
      with:
        source: ./docs
        destination: ./_site
    
    - name: ✅ Docs build passed
      run: |
        echo "🎉 GitHub Pages build successful!"
        echo "No Liquid syntax errors found."
